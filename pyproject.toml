[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "icd-c-code-refactorer"
version = "0.1.0"
description = "Transform C source code between ICD versions using local LLM inference"
requires-python = ">=3.10"